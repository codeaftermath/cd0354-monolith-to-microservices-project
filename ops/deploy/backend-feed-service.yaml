apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-api-feed
  name: backend-feed
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    run: udagram-api-feed
